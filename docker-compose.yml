version: "3.8"

services:
    postgres:
        image: postgres:latest
        container_name: buybuyin-postgres-db
        env_file:
            - ./server/.env
        ports:
            - "5432:5432"
        volumes:
            - postgres-data:/var/lib/postgresql

volumes:
    postgres-data:
